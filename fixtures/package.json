{
  "name": "fixtures",
  "private": true,
  "type": "module",
  "description": "Dev-only apps-under-test + mocks for exercising the synced-preview bridge. Not vendored, not shipped.",
  "dependencies": {
    "react": "^18.3.1",
    "react-aria-components": "^1.5.0",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
